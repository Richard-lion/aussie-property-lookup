services:
  - type: web
    name: aussie-property-api
    env: python
    pythonVersion: "3.11"
    buildCommand: pip install -r pyapi/requirements.txt
    startCommand: cd pyapi && uvicorn main:app --host 0.0.0.0 --port $PORT